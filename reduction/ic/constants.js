let baseSpeed = 3;
let timePerProblem = 30;
// This 3 and 24 is a bit inelegant but 40 minutes will give a lot
// fewer complaints than 50 minutes (shorter than 45, not longer).
let timePerPhase = 240;
let startingTime = 2400;
